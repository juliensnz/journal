{% extends 'base.html.twig' %}

{% block body %}
    <div class="header">
        {{ cards('journal.header') }}
    </div>
    <div class="articles">
        {{ cards('journal.article.learn') }}
        <span class="thin-split"></span>
        <span class="thin-split"></span>
        {{ cards('journal.article.game') }}
        <span class="thin-split"></span>
        <span class="thin-split"></span>
        {{ cards('journal.article.picture') }}
        {#<span class="thin-split"></span>
        <span class="thin-split"></span>
        {{ cards('journal.article.work') }}#}
    </div>
    <div class="footer">
        {{ cards('journal.footer') }}
    </div>
{% endblock %}
